{
  "name": "bytes-module",
  "version": "1.0.0",
  "description": "Pure, stateless, dependency-free IEC byte-count format/parse (human-readable B/KiB/MiB/GiB/TiB/PiB string, strict inverse, fail-closed).",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "main": "index.mjs",
  "scripts": {
    "test": "node --test"
  }
}
