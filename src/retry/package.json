{
  "name": "retry-module",
  "version": "1.0.0",
  "description": "Pure, zero-runtime-dependency async retry with exponential backoff + full jitter and injectable sleep/random seams.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "main": "./retry.ts",
  "exports": {
    ".": "./retry.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.8"
  }
}
