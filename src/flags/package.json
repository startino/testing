{
  "name": "flags-module",
  "version": "1.0.0",
  "description": "Pure, fail-closed, dependency-free sandbox feature flags (booleans, read once at process start). NOT the Station product feature flag.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "main": "flags.mjs",
  "scripts": {
    "test": "node --test"
  }
}
