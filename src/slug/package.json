{
  "name": "slug-module",
  "version": "1.0.0",
  "description": "Pure, stateless, dependency-free Unicode-aware slugify (NFKD diacritic-fold, fail-closed).",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "main": "slug.mjs",
  "scripts": {
    "test": "node --test"
  }
}
