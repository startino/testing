{
  "name": "pipe-module",
  "version": "1.0.0",
  "description": "Pure, stateless, dependency-free function-composition helpers (pipe/flow/compose/tap/identity, correct on empty-varargs edges).",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "main": "pipe.mjs",
  "scripts": {
    "test": "node --test"
  }
}
