{
  "name": "duration-module",
  "version": "1.0.0",
  "description": "Pure, stateless, dependency-free duration format/parse (compact d/h/m/s string, strict inverse, fail-closed).",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "main": "duration.mjs",
  "scripts": {
    "test": "node --test"
  }
}
