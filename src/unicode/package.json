{
  "name": "unicode-text-module",
  "version": "1.0.0",
  "description": "Pure, stateless, dependency-free Unicode-correct text processing (NFC, grapheme-aware length/slice).",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "main": "text.mjs",
  "scripts": {
    "test": "node --test"
  }
}
