# Station runtime state — per-session, not source
.station/

# Prose VM run artifacts, should never land in a project repo
.prose/
runs/
.agents/prose/runs/

# Installed dependencies — never source (self-contained src/ modules install their own)
node_modules/
package-lock.json

# OS / editor cruft
.DS_Store
*.swp
